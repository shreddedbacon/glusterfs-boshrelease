set -e

export PREFIX=${BOSH_INSTALL_TARGET}
export PATH=$PREFIX/bin:/var/vcap/packages/gluster-build-tools/bin:$PATH

# attr
tar xzf gluster-build-deps/attr-2.4.47.src.tar.gz
(
  set -e
  cd attr-2.4.47
  ./configure --prefix=$PREFIX
  make install install-dev install-lib
)

ls -alh ${BOSH_INSTALL_TARGET}/lib | grep libattr

# acl
tar xzf gluster-build-deps/acl-2.2.52.src.tar.gz
(
  set -e
  cd acl-2.2.52
  LDFLAGS=-L${PREFIX}/lib CFLAGS=-I${PREFIX}/include ./configure --prefix=$PREFIX
  LDFLAGS=-L${PREFIX}/lib CFLAGS=-I${PREFIX}/include make install install-dev install-lib
  #prefix=$PREFIX
)

#libaio
tar xzf gluster-build-deps/libaio-0.3.111.tar.gz
(
 set -e
 cd libaio-0.3.111
 make install prefix=$PREFIX
)

# libnl
tar xzf gluster-build-deps/libnl-3.4.0.tar.gz
(
  set -e
  cd libnl-3.4.0
  ./configure --prefix=$PREFIX
  make install
)


#libibverbs
tar xzf gluster-build-deps/libibverbs-1.2.1.tar.gz
(
  set -e
  cd libibverbs-1.2.1
  LDFLAGS=-L${PREFIX}/lib CFLAGS=-I${PREFIX}/include ./configure --prefix=$PREFIX
  LDFLAGS=-L${PREFIX}/lib CFLAGS=-I${PREFIX}/include make install prefix=$PREFIX
)

#librdmacm
tar xzf gluster-build-deps/librdmacm-1.1.0.tar.gz
(
  set -e
  cd librdmacm-1.1.0
  CFLAGS="-I$PREFIX/include" LDFLAGS="-L$PREFIX/include" ./configure --prefix=$PREFIX --includedir=$PREFIX/include --libdir=$PREFIX/lib --disable-libcheck
  make prefix=$PREFIX LIBS=-libverbs LDFLAGS=-L$PREFIX/lib
)

#flex
tar xzf gluster-build-deps/flex-2.5.33.tar.gz
(
  set -e
  cd flex-2.5.33
  ./configure --prefix=$PREFIX
  make install prefix=$PREFIX
)

#bison
tar xzf gluster-build-deps/bison-3.2.4.tar.gz
(
  set -e
  cd bison-3.2.4
  ./configure --prefix=$PREFIX --with-libiconv-prefix=/usr/local/libiconv/
  make install prefix=$PREFIX
)

#expat
tar xzf gluster-build-deps/expat-2.2.6.tar.bz2
(
  set -e
  cd expat-2.2.6
  ./configure --prefix=$PREFIX
  make install prefix=$PREFIX
)

# XML-Parser
tar xzf gluster-build-deps/XML-Parser-2.44.tar.gz
(
  set -e
  cd XML-Parser-2.44
  perl Makefile.PL PREFIX=$PREFIX EXPATLIBPATH=$PREFIX/lib EXPATINCPATH=$PREFIX/include
  make install prefix=$PREFIX
)

export PERL5LIB=/var/vcap/packages/gluster-build-deps/lib/perl/5.18.2

# intltool
tar xzf gluster-build-deps/intltool-0.51.0.tar.gz
(
  set -e
  cd intltool-0.51.0
  ./configure --prefix=$PREFIX
  make install prefix=$PREFIX
)

#userspace-rcu
tar xjf gluster-build-deps/userspace-rcu-0.10.2.tar.bz2
(
  set -e
  cd userspace-rcu-0.10.2
  ./configure --prefix=$PREFIX
  make install prefix=$PREFIX
)

#lvm2
tar xzf gluster-build-deps/LVM2.2.02.183.tgz
(
  set -e
  cd LVM2.2.02.183
  ./configure --prefix=$PREFIX    \
              --exec-prefix=      \
              --with-confdir=/etc \
              --enable-applib     \
              --enable-cmdlib     \
              --enable-pkgconfig
  make install prefix=$PREFIX
)
